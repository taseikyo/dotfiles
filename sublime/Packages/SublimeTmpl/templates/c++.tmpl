/**
 * @authors ${author} (${email})
 * @date    ${date}
 * @link    ${link}
 */

#include <stdio.h>
#include <vector>
#include <iostream>

using namespace std;

int main(int argc, char const *argv[]) {
	$0
	return 0;
}